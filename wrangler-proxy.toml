# Cloudflare Workers 配置文件 - 隐私防护反代服务
name = "privacy-proxy"
main = "proxy-worker.js"
compatibility_date = "2024-01-01"

# 路由配置（可选）
# routes = [
#   { pattern = "proxy.example.com/*", zone_name = "example.com" }
# ]

# 环境变量（可选）
# [vars]
# DEBUG = "false"
